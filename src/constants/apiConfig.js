export const API_CONFIG = {
  BASE_URL: "https://jsonplaceholder.typicode.com",
  SESSIONS_ENDPOINT: "/posts",
  DEFAULT_SESSIONS_LIMIT: 12,
};

export const FLAT_LIST_CONFIG = {
  initialNumToRender: 6,
  maxToRenderPerBatch: 6,
  windowSize: 5,
};
